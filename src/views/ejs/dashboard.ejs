<!DOCTYPE html>
<html lang="es">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>
        <%= title %>
    </title>
    <script src="https://cdn.jsdelivr.net/npm/axios/dist/axios.min.js"></script>
    <script src="https://cdn.jsdelivr.net/npm/sweetalert2@11"></script>
    <link href="https://fonts.googleapis.com/css2?family=Exo+2:wght@300;400;700&display=swap" rel="stylesheet">
    <style>
        /* --- Configuración Global y Fondo --- */
        :root {
            --primary-blue: #4d4dff;
            --primary-purple: #6f00ff;
            --dark-bg: #0a051a;
            --glass-bg: rgba(15, 10, 30, 0.7);
            --text-primary: #f0f0f0;
            --text-secondary: rgba(240, 240, 240, 0.7);
        }

        body {
            font-family: 'Exo 2', sans-serif;
            background: radial-gradient(ellipse at bottom, var(--dark-bg) 0%, #1a0a3a 50%, #2a0d5a 100%);
            background-color: var(--dark-bg);
            color: var(--text-primary);
            display: grid;
            place-items: center;
            min-height: 100vh;
            overflow: hidden;
            margin: 0;
        }

        /* --- Contenedor Principal del Dashboard --- */
        .dashboard-container {
            width: 90vw;
            height: 90vh;
            display: flex;
            background: var(--glass-bg);
            backdrop-filter: blur(12px);
            border: 1px solid var(--primary-blue);
            border-radius: 15px;
            box-shadow: 0 0 30px rgba(77, 77, 255, 0.5);
            overflow: hidden;
        }

        /* --- Barra Lateral y Perfil --- */
        .sidebar {
            flex-basis: 22%;
            padding: 2rem 1.5rem;
            border-right: 1px solid rgba(77, 77, 255, 0.3);
            display: flex;
            flex-direction: column;
        }

        .profile-section {
            text-align: center;
            margin-bottom: 3rem;
        }

        .profile-section img {
            width: 100px;
            height: 100px;
            border-radius: 50%;
            border: 3px solid var(--primary-blue);
            box-shadow: 0 0 15px var(--primary-blue);
            margin-bottom: 1rem;
        }

        /* --- ESTILOS ELIMINADOS --- */
        /* Se eliminó .nav-menu .nav-button */


        /* --- Contenido Principal y Tarjetas de Servicio --- */
        .main-content {
            flex-basis: 78%;
            padding: 2rem 3rem;
            overflow-y: auto;
        }

        /* --- NUEVO ESTILO: Cabecera Principal --- */
        .main-header {
            display: flex;
            justify-content: space-between;
            align-items: center;
            margin-bottom: 1.5rem;
            /* Espacio antes de la lista */
        }

        .main-header h2 {
            margin: 0;
            color: var(--text-primary);
            font-weight: 400;
            border-bottom: 1px solid var(--primary-purple);
            padding-bottom: 0.5rem;
        }

        /* --- NUEVO ESTILO: Botón en la cabecera --- */
        .main-header .nav-button {
            padding: 10px 16px;
            border: none;
            border-radius: 5px;
            background: var(--primary-blue);
            color: white;
            font-size: 0.9rem;
            /* Un poco más pequeño para la cabecera */
            font-weight: 700;
            font-family: 'Exo 2', sans-serif;
            cursor: pointer;
            transition: all 0.3s ease;
        }

        .main-header .nav-button:hover {
            background: var(--primary-purple);
            box-shadow: 0 0 15px var(--primary-purple);
        }

        /* --- NUEVO ESTILO: Mensaje de "No hay servicios" --- */
        .no-services-msg {
            display: none;
            /* Oculto por defecto */
            text-align: center;
            color: var(--text-secondary);
            font-size: 1.1rem;
            padding: 3rem 0;
        }

        /* ... (El resto de estilos de .service-card, .modal, etc., son iguales) ... */

        /* Tarjeta de Servicio Individual */
        .service-card {
            background: rgba(25, 15, 40, 0.8);
            /* Vidrio más oscuro */
            border: 1px solid var(--primary-purple);
            border-radius: 8px;
            padding: 1.5rem;
            margin-bottom: 1rem;
            display: flex;
            justify-content: space-between;
            align-items: center;
            transition: all 0.3s ease;

            /* Animación de entrada */
            opacity: 1;
            transform: scale(1);
            animation: fadeIn 0.5s ease;
        }

        @keyframes fadeIn {
            from {
                opacity: 0;
                transform: scale(0.95);
            }

            to {
                opacity: 1;
                transform: scale(1);
            }
        }

        .service-card:hover {
            box-shadow: 0 0 15px rgba(111, 0, 255, 0.5);
            transform: translateY(-3px);
        }

        .service-info h4 {
            margin: 0 0 0.5rem 0;
            font-size: 1.2rem;
            color: var(--text-primary);
        }

        .status {
            font-size: 0.8rem;
            padding: 3px 8px;
            border-radius: 12px;
            font-weight: 700;
        }

        .status.active {
            background: rgba(0, 204, 102, 0.2);
            color: #00cc66;
        }

        .status.inactive {
            background: rgba(255, 59, 48, 0.2);
            color: #ff3b30;
        }

        .action-btn {
            padding: 8px 12px;
            border: none;
            border-radius: 5px;
            font-family: 'Exo 2', sans-serif;
            cursor: pointer;
            font-weight: 700;
            margin-left: 0.5rem;
            transition: all 0.3s ease;
        }

        .action-btn.modify-btn {
            background: var(--primary-blue);
            color: white;
        }

        .action-btn.modify-btn:hover {
            background: #6666ff;
        }

        .action-btn.delete-btn {
            background: transparent;
            border: 1px solid #ff3b30;
            color: #ff3b30;
        }

        .action-btn.delete-btn:hover {
            background: #ff3b30;
            color: white;
        }

        /* --- Estilos del Modal (Añadir Servicio) --- */
        .modal {
            display: none;
            /* Oculto por JS */
            position: fixed;
            z-index: 100;
            left: 0;
            top: 0;
            width: 100%;
            height: 100%;
            background: rgba(0, 0, 0, 0.6);
            backdrop-filter: blur(5px);
            justify-content: center;
            align-items: center;
            animation: fadeInModal 0.3s;
        }

        @keyframes fadeInModal {
            from {
                opacity: 0;
            }

            to {
                opacity: 1;
            }
        }

        .modal-content {
            background: #1a0a3a;
            padding: 2rem 3rem;
            border-radius: 10px;
            border: 1px solid var(--primary-blue);
            box-shadow: 0 0 30px var(--primary-blue);
            min-width: 350px;
            position: relative;
        }

        .close-btn {
            position: absolute;
            top: 15px;
            right: 20px;
            color: var(--text-secondary);
            font-size: 2rem;
            font-weight: bold;
            cursor: pointer;
        }

        .close-btn:hover {
            color: var(--text-primary);
        }

        .available-services-list {
            list-style: none;
            padding: 0;
            margin-top: 2rem;
        }

        .available-services-list li {
            padding: 12px 15px;
            margin-bottom: 0.5rem;
            background: rgba(255, 255, 255, 0.05);
            border-radius: 5px;
            border-left: 3px solid var(--primary-purple);
            cursor: pointer;
            transition: all 0.3s ease;
        }

        .available-services-list li:hover {
            background: var(--primary-blue);
            color: white;
            transform: translateX(5px);
        }

        .pre_name_serv_num {
            scale: 0.9;
            display: inline-flex;
            margin: 0;
            background-color: #f4f4f410;
            border: 1px solid #ddd1;
            border-radius: 5px;
            padding: .1rem .5rem;
            padding-right: .55rem;
            overflow-x: auto;
        }

        .pre_name_serv_num code {
            font-family: 'Consolas', 'Menlo', 'Courier New', monospace;
            font-size: 0.9rem;
            line-height: 1.5;
            opacity: .5;
        }
    </style>
</head>

<body>

    <div class="dashboard-container">

        <aside class="sidebar">
            <div class="profile-section">
                <img src="./img/ktdi.jpeg" alt="Avatar">
                <h3>
                    <%= user_data.basics.name %>
                </h3>
                <p>ID: <%= user_data.basics.id %>
                </p>
            </div>

        </aside>

        <main class="main-content">

            <div class="main-header">
                <h2>Servicios Vinculados</h2>
                <button id="add-service-btn" class="nav-button">
                    Añadir Servicio
                </button>
            </div>

            <div id="service-list">
                <% const srv=user_data.services.services; if (!srv || srv.length <=0) { %>
                    <p id="no-services-msg" class="no-services-msg">
                        No hay servicios registrados
                    </p>
                    <% } else { for (let i=0; i<srv.length; i++) { %>
                        <div class="service-card %>">
                            <div class="service-info">
                                <h4>
                                    <%= srv[i].name %>&nbsp;
                                        <pre class="pre_name_serv_num"><code>#<%= srv[i].n_rep %></code></pre>
                                </h4>
                                <span class="status active">Done</span>
                                <input type="hidden" id="element_id" value="<%= srv[i].UUID %>">
                            </div>
                            <div class="card-actions">
                                <input type="hidden" id="cf_id" value="<%= JSON.stringify(srv[i].config_data) %>">
                                <button class="show_keys_service action-btn modify-btn">Ver Keys</button>
                                <button class="delete_service action-btn delete-btn">Eliminar</button>
                            </div>
                        </div>
                        <% } } %>
            </div>
        </main>

    </div>

    <div id="add-service-modal" class="modal">
        <div class="modal-content">
            <span class="close-btn">&times;</span>
            <h3>Añadir Nuevo Servicio</h3>

            <ul class="available-services-list">
                <% for (let i=0; i < services.length; i++) { %>
                    <% if (services[i].activated==true) { %>
                        <li class="<% if (!services[i].UUID) { %>dontclickevent<% } %> element-<%= i %>-name">
                            <%= services[i].name %>
                        </li>
                        <input type="hidden" class="element-<%= i %>-info" value="<%= services[i].UUID %>">
                        <% } %>
                            <% } %>
            </ul>
        </div>
    </div>

</body>

</html>

<script>
    window.addEventListener('pageshow', (event) => {
        if (event.persisted) {
            window.location.reload();
        }
    });

    document.addEventListener('DOMContentLoaded', () => {

        const addServiceBtn = document.getElementById('add-service-btn');
        const modal = document.getElementById('add-service-modal');
        const closeModalBtn = modal.querySelector('.close-btn');
        const serviceList = document.getElementById('service-list');
        const availableServices = modal.querySelector('.available-services-list');

        const noServicesMsg = document.getElementById('no-services-msg');

        function updateServiceListState() {
            const services = serviceList.querySelectorAll('.service-card');
            if (noServicesMsg) {
                if (services.length === 0) {
                    noServicesMsg.style.display = 'block';
                } else {
                    noServicesMsg.style.display = 'none';
                }
            }
        }

        addServiceBtn.addEventListener('click', () => {
            modal.style.display = 'flex';
        });
        closeModalBtn.addEventListener('click', () => {
            modal.style.display = 'none';
        });
        window.addEventListener('click', (e) => {
            if (e.target === modal) {
                modal.style.display = 'none';
            }
        });

        availableServices.addEventListener('click', async (e) => {
            if (e.target.tagName === 'LI' && !e.target.classList.contains("dontclickevent")) {
                const serviceName = e.target.textContent

                modal.style.display = 'none'

                Swal.fire({
                    icon: 'info',
                    title: 'Procesando Peticion',
                    showConfirmButton: false,
                    background: 'rgba(15, 10, 30, 1)'
                })

                let clsTar = `.${String(e.target.classList).trim()}`

                clsTar = clsTar.substring(0, clsTar.length - 4) + 'info'

                const UUID_service = document.querySelector(clsTar).value;
                const service_name = serviceName
                const service_name_formatted = service_name.trim().replace(/\s+/g, '_').toLowerCase();

                try {
                    const response = await axios.post('/service/configurations/get',
                        {
                            UUID: UUID_service,
                            name: service_name_formatted
                        }
                    );

                    try {
                        if (response && response.status == 200 && response.data.status == 200) {
                            Swal.fire({
                                icon: 'success',
                                title: 'Servicio Añadido',
                                html: `<p>El servicio "<strong>${serviceName}</strong>" ha sido añadido correctamente.</p>`,
                                showConfirmButton: true,
                                confirmButtonText: 'Show Config Keys',
                                background: 'rgba(15, 10, 30, 1)',
                                showCancelButton: true,
                                cancelButtonText: 'Cerrar'

                            }).then((result) => {
                                if (result.isConfirmed) {
                                    const rdb = response.data
                                    const accessKey = rdb.config_data.access_key
                                    const accessPassword = rdb.config_data.access_password
                                    const endpointUrl = rdb.config_data.EndPoint
                                    const example_input = rdb.example_input
                                    const example_output = rdb.example_output

                                    const handleCopyClick = (textToCopy, buttonElement) => {
                                        navigator.clipboard.writeText(textToCopy).then(() => {
                                            const originalText = buttonElement.innerText
                                            buttonElement.innerText = '¡Copiado!'
                                            setTimeout(() => {
                                                buttonElement.innerText = originalText
                                            }, 2000);
                                        }).catch(err => { })
                                    };

                                    Swal.fire({
                                        title: 'Config Keys',
                                        icon: 'info',
                                        html: `
                                            <p>Aquí están las claves para <strong>configurar tu ESP32</strong>:</p>
                                            <br>
                                            <div style="text-align: left; margin: 10px 0;">
                                                <p><strong>Access Key:</strong> ${accessKey.substring(0, 8)}... 
                                                    <button class="copy-btn swal2-styled swal2-default" data-copy-text="${accessKey}" style="margin-left: 10px; font-size: 0.8em; padding: 5px 10px; background: #7066e0; color: #bfbfbf;">Copiar</button>
                                                </p>
                                                <p><strong>Access Password:</strong> ${accessPassword.substring(0, 8)}...
                                                    <button class="copy-btn swal2-styled swal2-default" data-copy-text="${accessPassword}" style="margin-left: 10px; font-size: 0.8em; padding: 5px 10px; background: #7066e0; color: #bfbfbf;">Copiar</button>
                                                </p>
                                                <p><strong>Endpoint URL:</strong> ${endpointUrl.substring(0, 8)}...
                                                    <button class="copy-btn swal2-styled swal2-default" data-copy-text="${endpointUrl}" style="margin-left: 10px; font-size: 0.8em; padding: 5px 10px; background: #7066e0; color: #bfbfbf;">Copiar</button>
                                                </p>
                                                <p><strong>Input Example:</strong> {...}
                                                    <button class="copy-btn swal2-styled swal2-default" data-copy-text="${example_input}" style="margin-left: 10px; font-size: 0.8em; padding: 5px 10px; background: #7066e0; color: #bfbfbf;">Copiar</button>
                                                </p>
                                                <p><strong>Output Example:</strong> {...}
                                                    <button class="copy-btn swal2-styled swal2-default" data-copy-text="${example_output}" style="margin-left: 10px; font-size: 0.8em; padding: 5px 10px; background: #7066e0; color: #bfbfbf;">Copiar</button>
                                                </p>
                                            </div>
                                        `,
                                        confirmButtonText: 'Cerrar',
                                        background: 'rgba(15, 10, 30, 1)',
                                        didOpen: () => {
                                            document.querySelectorAll('.copy-btn').forEach(button => {
                                                button.addEventListener('click', () => {
                                                    const text = button.getAttribute('data-copy-text')
                                                    handleCopyClick(text, button)
                                                })
                                            })
                                        }
                                    })
                                } else {
                                    location.reload(true)
                                }
                            });
                            updateServiceListState()
                        } else {
                            Swal.fire({
                                icon: 'error',
                                title: 'Servicio Añadido',
                                html: `hubo un error al intentar configurar tu servicio`,
                                position: "center",
                                showConfirmButton: true,
                                focusConfirm: true,
                                background: 'rgba(15, 10, 30, 1)',
                                timer: 3500,
                                timerProgressBar: true,
                                customClass: {
                                    title: 'swal-title-custom_failed',
                                    htmlContainer: 'swal-text-custom'
                                }
                            })
                        }
                    } catch (error) {
                        Swal.fire({
                            icon: 'error',
                            title: 'Servicio Añadido',
                            html: `hubo un error al intentar configurar tu servicio`,
                            position: "center",
                            showConfirmButton: true,
                            focusConfirm: true,
                            background: 'rgba(15, 10, 30, 1)',
                            timer: 3500,
                            timerProgressBar: true,
                            customClass: {
                                title: 'swal-title-custom_failed',
                                htmlContainer: 'swal-text-custom'
                            }
                        })
                    }
                } catch (error) { }
            }
        })

        document.querySelectorAll('.delete_service').forEach((e) => {
            e.addEventListener('click', function (obj) {
                const uuid_service = [...[...e.parentElement.parentElement.children].filter(element => { if (element.className === "service-info") return element })[0].children].filter(element => { return element.tagName === 'INPUT' && element.type === 'hidden' })[0].value
                const s_name_id_parent = [...[...e.parentElement.parentElement.children].filter(element => { if (element.className === "service-info") return element })[0].children].filter(element => { return element.tagName === 'H4' })[0]
                const s_name = s_name_id_parent.firstChild.textContent.trim()
                const s_identifier = s_name_id_parent.firstElementChild.textContent.trim()
                const src_title = `${s_name} ${s_identifier}`

                Swal.fire({
                    icon: 'warning',
                    title: 'Deseas Eliminar el Acceso a Este Servicio?',
                    html: `Deshabilitar el Servicio <pre class="pre_name_serv_num">${src_title}<code></code></pre>`,
                    position: "center",
                    showConfirmButton: true,
                    confirmButtonText: 'Eliminar Acceso',
                    showCancelButton: true,
                    cancelButtonText: 'Cancelar',
                    focusConfirm: true,
                    background: 'rgba(15, 10, 30, 1)',
                    buttonsStyling: false,
                    customClass: {
                        title: 'swal-title-custom_failed',
                        htmlContainer: 'swal-text-custom',
                        confirmButton: 'action-btn modify-btn',
                        cancelButton: 'action-btn delete-btn'
                    }
                }).then((opt) => {
                    if (opt.isDismissed) {
                        console.log('no borra')
                    } else if (opt.isConfirmed) {
                        console.log('borra')
                        Swal.fire({
                            icon: 'success',
                            title: 'Servicio Deshabilitado',
                            html: `
                                <p>El acceso al servicio </p>
                                <pre class="pre_name_serv_num">${src_title}<code></code></pre>
                                <p>Ha sido deshabilitado correctamente.</p>
                            `,
                            showConfirmButton: true,
                            confirmButtonText: 'Ok',
                            background: 'rgba(15, 10, 30, 1)',
                            customClass: {
                                title: 'swal-title-custom_failed',
                                htmlContainer: 'swal-text-custom',
                                confirmButton: 'action-btn modify-btn'
                            }

                        })
                    }
                })
                // Swal.fire(uuid_service)
            })
        })

        document.querySelectorAll('.show_keys_service').forEach((e) => {
            e.addEventListener('click', function (obj) {
                try {
                    const cf_data = JSON.parse([...[...e.parentElement.parentElement.children].filter(element => { if (element.className === "card-actions") return element })[0].children].filter(element => { return element.tagName === "INPUT" && element.type === "hidden" })[0].value)

                    const rdb = cf_data
                    const accessKey = rdb.access_key
                    const accessPassword = rdb.access_password
                    const endpointUrl = rdb.endpoint_url
                    const example_input = 'No Disponible'
                    const example_output = 'No Disponible'

                    const handleCopyClick = (textToCopy, buttonElement) => {
                        navigator.clipboard.writeText(textToCopy).then(() => {
                            const originalText = buttonElement.innerText
                            buttonElement.innerText = '¡Copiado!'
                            setTimeout(() => {
                                buttonElement.innerText = originalText
                            }, 2000);
                        }).catch(err => { })
                    };

                    Swal.fire({
                        title: 'Config Keys',
                        icon: 'info',
                        html: `
                                <p>Aquí están las claves para <strong>configurar tu ESP32</strong>:</p>
                                <br>
                                <div style="text-align: left; margin: 10px 0;">
                                    <p><strong>Access Key:</strong> ${accessKey.substring(0, 8)}... 
                                        <button class="copy-btn swal2-styled swal2-default" data-copy-text="${accessKey}" style="margin-left: 10px; font-size: 0.8em; padding: 5px 10px; background: #7066e0; color: #bfbfbf;">Copiar</button>
                                    </p>
                                    <p><strong>Access Password:</strong> ${accessPassword.substring(0, 8)}...
                                        <button class="copy-btn swal2-styled swal2-default" data-copy-text="${accessPassword}" style="margin-left: 10px; font-size: 0.8em; padding: 5px 10px; background: #7066e0; color: #bfbfbf;">Copiar</button>
                                    </p>
                                    <p><strong>Endpoint URL:</strong> ${endpointUrl.substring(0, 8)}...
                                        <button class="copy-btn swal2-styled swal2-default" data-copy-text="${endpointUrl}" style="margin-left: 10px; font-size: 0.8em; padding: 5px 10px; background: #7066e0; color: #bfbfbf;">Copiar</button>
                                    </p>
                                    <p><strong>Input Example:</strong> {...}
                                        <button class="copy-btn swal2-styled swal2-default" data-copy-text="${example_input}" style="margin-left: 10px; font-size: 0.8em; padding: 5px 10px; background: #7066e0; color: #bfbfbf;" disabled>Copiar</button>
                                    </p>
                                    <p><strong>Output Example:</strong> {...}
                                        <button class="copy-btn swal2-styled swal2-default" data-copy-text="${example_output}" style="margin-left: 10px; font-size: 0.8em; padding: 5px 10px; background: #7066e0; color: #bfbfbf;" disabled>Copiar</button>
                                    </p>
                                </div>
                            `,
                        confirmButtonText: 'Cerrar',
                        background: 'rgba(15, 10, 30, 1)',
                        didOpen: () => {
                            document.querySelectorAll('.copy-btn').forEach(button => {
                                button.addEventListener('click', () => {
                                    const text = button.getAttribute('data-copy-text')
                                    handleCopyClick(text, button)
                                })
                            })
                        }
                    })
                } catch (error) { console.log(error) }
            })
        })

        updateServiceListState()
    })
</script>